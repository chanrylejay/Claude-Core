# Frozen archives hold client names, terms, and compensation details.
# They stay LOCAL-ONLY (plus offline backup) — never committed, even to a private repo.
# Chan's ruling, Jul 24 2026. Keep this exclusion forever.
archives/
LOCAL-ONLY-*
memory/LOCAL-ONLY-*
.env*
*.pem
*.key
*credential*
*.zip
settings.json
settings.local.json
*.bak*
# VS Code multi-root workspace files are Chan's local tools, never repo content (ruling Aug 24 2026).
*.code-workspace
# Playwright CLI's local, disk-first snapshots are proof artifacts, never kit source.
.playwright-cli/
