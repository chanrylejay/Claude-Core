---
name: boss-operating-protocol
description: RETIRED INTO POINTERS (core-shrink slice 1, Aug 24 2026, Chan's token-efficiency GO). The drill and memory mechanics live in workflow/the-drill-and-memory.md; the cadence, QA gate, and roles live in the frozen core (Claude-Core/CLAUDE.md); banking law in the drill home. This file survives as a pointer so old links resolve; deleting it entirely is a proposed LATER slice needing Chan's explicit OK (deleting a banked file is the strongest act the memory law names).
metadata:
  node_type: memory
  type: feedback
---

# Boss operating protocol — retired into its homes (Aug 24 2026)

Everything this file carried now lives in exactly two places: **THE DRILL and the memory
system** in `../workflow/the-drill-and-memory.md` (the authoritative home it always pointed
at), and **the session cadence, roles, and QA gate** in the frozen core
(`Claude-Core/CLAUDE.md`). One line survives here because it lived nowhere else: **orient
first** — project MEMORY.md, repo CLAUDE.md, and [[chan-hard-rules]] before acting; those
files are the fastest orientation, never proof of current state — disk wins, at a start AND
mid-session.
